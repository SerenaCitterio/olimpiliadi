# ── Google Sheets integration ──────────────────────────────
#
# Setup (una tantum):
# 1. Vai su https://console.cloud.google.com
# 2. Crea un nuovo progetto (o usa uno esistente)
# 3. Abilita "Google Sheets API" in APIs & Services
# 4. Crea un Service Account in IAM & Admin > Service Accounts
# 5. Genera una chiave JSON per il Service Account
# 6. Condividi il Google Sheet con l'email del Service Account (permesso "Visualizzatore")
# 7. Copia i valori qui sotto dal file JSON scaricato
#
# Il foglio deve avere 3 tab:
#   - "Squadre": id | name | emoji | defender | attacker | groupId | groupName
#   - "Partite": id | team1Id | team2Id | score1 | score2 | played | date | roundLabel | status
#   - "Bracket": id | team1Id | team2Id | score1 | score2 | played | round | side
#
# Se queste variabili non sono impostate, il sito usa i dati mock di sviluppo.
# Per forzare i dati mock anche con le credenziali configurate (utile in sviluppo locale):
#   USE_MOCK_DATA=true

USE_MOCK_DATA=false

GOOGLE_SHEETS_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=